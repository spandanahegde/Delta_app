<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Main app theme -->
    <style name="Theme.DeltaSiteManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Your regular app colors and settings -->
    </style>

    <!-- Splash theme (startup only) -->
    <style name="Theme.DeltaSiteManager.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
